# Smart Hub Ultra MAXX

🚀 Voice-controlled AI bot builder + Python executor in-browser + modular backend = Supreme Dev Power.

## Updated Concept
- **Capture**: Turn voice prompts into runnable Python templates without leaving the browser.
- **Compose**: Mix-and-match weather, messaging, and ML primitives via ready-made snippets.
- **Execute**: Skulpt powers instant client-side execution; Flask serves the ML sentiment endpoint.
- **Deliver**: Offline-first PWA shell that pre-caches core assets for quick reloads.

## Features
- 🎤 Voice-based bot creation with live status feedback
- 🧠 Run Python in-browser (Skulpt) with runtime telemetry
- 📦 Click-to-insert component snippets
- 🧬 Flask ML backend
- 🗃 Bot creation log
- 📡 Service worker caching for faster startup

## Use

### Frontend
```bash
live-server
```

### Backend
```bash
pip install -r requirements.txt
cd backend
python app.py
```
